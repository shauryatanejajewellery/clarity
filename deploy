#!/bin/bash
set -e
REPO="$(cd "$(dirname "$0")" && pwd)"
cp ~/Downloads/index.html "$REPO/index.html"
cd "$REPO"
git add index.html
git diff --cached --quiet && echo "Nothing new to commit." && exit 0
git commit -m "Update index.html"
git push
echo "Deployed."
